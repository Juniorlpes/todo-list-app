name: todo_list
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.0
  get_it: ^7.6.4
  go_router: ^13.0.0
  hive_flutter: ^1.1.0
  path_provider: ^2.1.0
  xid: ^1.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.0
  hive_generator: ^2.0.0
  mocktail: ^0.3.0

flutter:
  uses-material-design: true
